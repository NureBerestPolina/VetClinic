import { Specialty } from "./specialty.model";

export interface Owner {
    ownerId: number;
    firstName: string;
    lastName: string;
    password: Specialty;
    phone: string;
    email: string;
}